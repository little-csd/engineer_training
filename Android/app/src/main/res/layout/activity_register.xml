<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".login.RegisterActivity">

    <ImageView
        android:layout_width="160dp"
        android:layout_height="160dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/main_page_icon"
        app:layout_constraintBottom_toTopOf="@id/input_nickname"
        android:layout_marginBottom="50dp"/>

    <EditText
        android:id="@+id/input_nickname"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toTopOf="@id/input_username_reg"
        android:layout_marginHorizontal="50dp"
        android:hint="@string/nickname"/>

    <EditText
        android:id="@+id/input_username_reg"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:inputType="text"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginHorizontal="50dp"
        android:hint="@string/username"/>

    <EditText
        android:id="@+id/input_password_reg"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:inputType="textPassword"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/input_username_reg"
        android:layout_marginHorizontal="50dp"
        android:hint="@string/password"/>

    <EditText
        android:id="@+id/input_password_reg2"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:inputType="textPassword"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/input_password_reg"
        android:layout_marginHorizontal="50dp"
        android:hint="@string/password"/>

    <Button
        android:id="@+id/btn_register_confirm"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:text="@string/confirm"
        android:textAllCaps="false"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/input_password_reg2"
        android:layout_marginHorizontal="50dp" />

</androidx.constraintlayout.widget.ConstraintLayout>